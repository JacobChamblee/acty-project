{
  "name": "cactus-insights",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && BUILD_PATH=../build npm run build",
    "start": "cd frontend && npm start"
  }
}
